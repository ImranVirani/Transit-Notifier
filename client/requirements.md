## Packages
geolib | For calculating distances between coordinates accurately

## Notes
- PWA manifest and service worker registration are handled by the build system/vite plugins if configured, but for this frontend scope we focus on the UI and logic.
- Geolocation API is browser native.
- Notification API is browser native.
- Using 'geolib' for robust Haversine distance calculations.
